{
  "name": "refocus-physical-ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:vercel": "node scripts/build-vercel.mjs",
    "monitor": "node web-monitor/server.mjs",
    "test": "node --test tests/*.test.mjs && python -m unittest tests/test_product_skill_contracts.py -v"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "1.0.1",
    "@tesseract.js-data/chi_sim": "^1.0.0",
    "tesseract.js": "^7.0.0"
  }
}
