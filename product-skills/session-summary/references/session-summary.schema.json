{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://refocus.local/schemas/session-summary-v2.json",
  "title": "RE:FOCUS session summary contract",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "skill",
    "goal",
    "outcome",
    "summary",
    "completed_items",
    "next_action",
    "focus_minutes_actual",
    "interruptions"
    ,"user_feedback"
  ],
  "properties": {
    "schema_version": { "const": "2.0" },
    "skill": { "const": "session-summary" },
    "goal": { "type": "string", "minLength": 1, "maxLength": 240 },
    "outcome": { "enum": ["completed", "partial", "cancelled", "unknown"] },
    "summary": { "type": "string", "minLength": 1, "maxLength": 500 },
    "completed_items": {
      "type": "array",
      "maxItems": 6,
      "items": { "type": "string", "minLength": 1, "maxLength": 160 }
    },
    "next_action": { "type": ["string", "null"], "minLength": 1, "maxLength": 240 },
    "focus_minutes_actual": { "type": ["integer", "null"], "minimum": 0, "maximum": 1440 },
    "interruptions": {
      "type": "object",
      "additionalProperties": false,
      "required": ["count", "total_seconds", "main_reason"],
      "properties": {
        "count": { "type": "integer", "minimum": 0 },
        "total_seconds": { "type": "number", "minimum": 0 },
        "main_reason": {
          "type": ["string", "null"],
          "enum": ["absent", "off_task", "idle", null]
        }
      }
    },
    "user_feedback": {
      "type": "object",
      "additionalProperties": false,
      "required": ["completion_report", "focus_experience"],
      "properties": {
        "completion_report": { "type": ["string", "null"], "minLength": 1, "maxLength": 300 },
        "focus_experience": { "type": ["string", "null"], "minLength": 1, "maxLength": 300 }
      }
    }
  }
}
